Track 4. Your tool goes in `<handle>/tool.py`, called with `--run <dir>` and
`--out <dir>`. You can add what you need to `environment.yml`; don't change the
pinned versions.

`<handle>` is YOUR INSTITUTE ROLL NUMBER, exactly as you gave it on the
application form. A directory under any other name is not collected.

THE EXAMPLE BESIDE THIS FILE IS A WORKING SUBMISSION, under the placeholder
handle `EXAMPLE`. Copy it, rename it to your roll number, and replace the
contents. It is deliberately poor -- it is the shape, not the answer.
